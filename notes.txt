sudo apt-get install nodejs
sudo apt install npm
npm init -y
node index.js
npm install -g nodemon
(npm install --save-dev nodemon -> pk aparegui al package json)
npm install express
--------------------DOCKERITZAR-----------------------
1. construeixes Dockerfile, important:
"scripts": {
  "dev": "nodemon server.js"
}
2. construeixes docker compose
3. sudo docker compose up --build
(matar processos sudo lsof -i :8000, sudo kill -9 PID)
4.  sudo docker compose down -v
5.sudo docker ps -a 
6. sudo docker rm DOCKERID -f

---------------------VUE------------------
npm create vue@latest
npm run dev
npm install cors -> per permetre que El navegador bloqueja la crida perquè són diferents orígens i el backend no permet explícitament que el frontend faci fetch.

---------------PER ENTRAR A MARIADB-----

sudo docker exec -it mariadb mariadb -u root -p
SHOW DATABASES
USE user;
SELECT * FROM User
